// Решение 1: Использование let (создает блочную область видимости для каждой итерации)

for (let i = 0; i < 10; i++) {
  setTimeout(function () {
    console.log(i); // Выведет числа от 0 до 9 по порядку
  });
}
