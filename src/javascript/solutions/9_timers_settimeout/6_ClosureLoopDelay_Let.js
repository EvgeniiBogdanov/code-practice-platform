// Вариант 1 (Рекомендуемый): Использование let
// Код выведет: 1 (через 1с), 2 (через 2с), 3 (через 3с)

for (let i = 1; i <= 3; i++) {
  setTimeout(function() {
    console.log(i);
  }, i * 1000);
}
