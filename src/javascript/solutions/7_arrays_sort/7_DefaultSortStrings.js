// sort() по умолчанию сортирует элементы как строки в лексикографическом порядке (по кодам Unicode)!
// Сначала "1", потом "100", потом "21", потом "30", потом "4"
const numbers = [1, 30, 4, 21, 100];
const result = numbers.sort();

console.log(result); // [1, 100, 21, 30, 4]
