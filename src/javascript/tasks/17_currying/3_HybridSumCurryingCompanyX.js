/** Реализуйте функцию sum, которая может быть вызвана двумя способами:
 * 1) sum(a, b) — возвращает сумму двух чисел сразу
 * 2) sum(a)(b) — возвращает функцию, которая принимает второй аргумент
 *    и возвращает сумму (Company X)
 */
const sum = (a, b) => {
  // Ваш код здесь
};

console.log(sum(1, 2)); // 3
console.log(sum(1)(2)); // 3
console.log(sum(5)(10)); // 15
