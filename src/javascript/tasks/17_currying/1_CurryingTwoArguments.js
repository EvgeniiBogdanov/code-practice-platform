// Каррирование суммы с двумя аргументами
// Напишите функцию sum, которая поддерживает варианты вызова: sum(1, 2) и sum(1)(2).

const sum = (val1, val2) => {
  // Решение тут
};

// Пример вызова:
console.log(sum(1, 2)); // 3
console.log(sum(1)(2)); // 3
