// Каррирование функции двух аргументов
// Напишите функцию multiply(a)(b), перемножающую два числа.

const multiply = (a) => {
  // Решение тут
};

// Пример вызова:
console.log(multiply(2)(3)); // 6
console.log(multiply(4)(5)); // 20
