// Бесконечное каррирование
// Напишите функцию суммирования, которая накапливает результат до вызова без аргумента ()

const curry_sum = (x) => {
  // Ваш код здесь
};

console.log(curry_sum(1)()); // 1
console.log(curry_sum(1)(2)(3)()); // 6
