// Бесконечное каррирование с завершением вызовом без аргументов
// Напишите функцию sum, поддерживающую цепочку sum(1)(2)(3)().

const sum = (a) => {
  // Решение тут
};

// Пример вызова:
console.log(sum(1)(2)(3)()); // 6
console.log(sum(5)(-2)());   // 3
