// Создайте Map сразу при инициализации, передав в конструктор
// массив пар [ключ, значение]:
// [["name", "Иван"], ["age", 30], ["city", "Москва"]]

const map = null;

console.log(map?.get("name")); // "Иван"
