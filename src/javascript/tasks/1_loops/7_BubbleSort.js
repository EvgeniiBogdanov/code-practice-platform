// Напишите функцию bubbleSort, которая принимает массив чисел и сортирует его по возрастанию, используя алгоритм
// пузырьковой сортировки. Верните отсортированный массив.

// Пример:
// Вход: [64, 34, 25, 12, 22, 11, 90]
// Выход: [11, 12, 22, 25, 34, 64, 90]

const arr = [64, 34, 25, 12, 22, 11, 90];

const bubbleSort = (arr) => {};

const result = bubbleSort(arr);
console.log(result);
