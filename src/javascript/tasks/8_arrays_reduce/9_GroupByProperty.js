// Напишите функцию groupBy(arr, key), которая группирует массив объектов по указанному ключу.
// Результат — объект, где ключи — значения свойства key, а значения — массивы объектов.

const people = [
  { age: 20, name: "Alice" },
  { age: 30, name: "Brat" },
  { age: 20, name: "Sem" },
];

const groupBy = (arr, key) => {
  // Решение тут
};

console.log(groupBy(people, "age"));
