// Напишите функцию sum(arr), которая с помощью reduce возвращает сумму всех элементов числового массива.
// Для пустого массива должен вернуться 0.

// Вход → Выход
// sum([1, 2, 3, 4, 5]) → 15
// sum([10, -5, 3]) → 8
// sum([]) → 0

const sum = (arr) => {
  // Решение тут
};

console.log(sum([1, 2, 3, 4, 5]));
console.log(sum([10, -5, 3]));
console.log(sum([]));
