// Напишите функцию unique(arr), которая через reduce возвращает массив только уникальных элементов,
// сохраняя порядок первого вхождения.

// Вход → Выход
// unique([1, 2, 1, 3, 2, 4]) → [1, 2, 3, 4]
// unique(['a', 'b', 'a', 'a', 'c']) → ['a', 'b', 'c']

const unique = (arr) => {
  // Решение тут
};

console.log(unique([1, 2, 1, 3, 2, 4]));
console.log(unique(["a", "b", "a", "a", "c"]));
