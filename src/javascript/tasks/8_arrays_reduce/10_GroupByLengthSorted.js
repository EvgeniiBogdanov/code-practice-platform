// Сгруппировать слова по длине, отсортировать внутри групп по алфавиту
// Вывод: { '3': ['бар', 'фoo'], '4': ['тест'], '6': ['привет'] }

const words = ["тест", "привет", "фoo", "бар"];

const groupBy = (arr) => {
  // Решение тут
};

console.log(groupBy(words));
