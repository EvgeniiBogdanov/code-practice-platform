// Отсортировать пользователей по возрасту (от младшего к старшему):
const users = [
  { name: "John", age: 25 },
  { name: "Jane", age: 20 },
  { name: "Jack", age: 30 }
];

// Тут код:
const sortByAge = () => {};

// Проверка
const result = sortByAge(users);
console.log(result);
