// Перестановка элементов массива (чётные вперед, нечётные назад)
// Напишите функцию rearrangeArray(arr), которая возвращает новый массив, где:
// 1. Все чётные числа идут перед нечётными
// 2. Чётные и нечётные внутри своих групп отсортированы по возрастанию

const rearrangeArray = (arr) => {
  // Решение тут
};

// Пример вызова:
console.log(rearrangeArray([5, 3, 2, 8, 1, 4])); // [2, 4, 8, 1, 3, 5]
