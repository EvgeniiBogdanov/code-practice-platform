// Исправление замыкания в цикле по массиву
// Напишите исправленный вариант с let, выводящий индексы и элементы.

const arr = [10, 12, 15, 21];

for (var i = 0; i < arr.length; i++) {
  setTimeout(() => {
    // Решение тут
  }, 100);
}
