// Наибольший общий делитель (Алгоритм Евклида)
// Напишите функцию gcd(a, b), которая находит наибольший общий делитель двух чисел a и b
// с помощью цикла while по алгоритму Евклида.

const gcd = (a, b) => {
  // Решение тут
};

// Пример вызова:
console.log(gcd(48, 18)); // 6
console.log(gcd(101, 10)); // 1
console.log(gcd(56, 98));  // 14
