// Сумма цифр числа
// Напишите функцию sumOfDigits(num), которая принимает неотрицательное целое число num
// и возвращает сумму его цифр с помощью цикла while (без перевода числа в строку).
// Пример: sumOfDigits(1234) => 10, sumOfDigits(5) => 5

const sumOfDigits = (num) => {
  // Ваш код здесь
};

// Примеры вызова:
console.log(sumOfDigits(1234)); // 10
console.log(sumOfDigits(5));    // 5
console.log(sumOfDigits(9081)); // 18
