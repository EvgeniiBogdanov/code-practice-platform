// Разворот числа
// Напишите функцию reverseNumber(num), которая принимает положительное число num
// и возвращает число с обратным порядком цифр с помощью цикла while.
// Пример: reverseNumber(12345) => 54321, reverseNumber(980) => 89

const reverseNumber = (num) => {
  // Ваш код здесь
};

// Примеры вызова:
console.log(reverseNumber(12345)); // 54321
console.log(reverseNumber(980));   // 89
console.log(reverseNumber(7));     // 7
