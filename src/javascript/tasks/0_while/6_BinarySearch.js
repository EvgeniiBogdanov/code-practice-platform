// Бинарный поиск
// Напишите функцию binarySearch(arr, target), которая находит индекс элемента target
// в отсортированном по возрастанию массиве arr за время O(log N).
// Если элемент не найден, возвращает -1.

const binarySearch = (arr, target) => {
  // Решение тут
};

// Пример вызова:
console.log(binarySearch([1, 3, 5, 7, 9, 11], 7)); // 3
console.log(binarySearch([1, 3, 5, 7, 9, 11], 2)); // -1
console.log(binarySearch([10, 20, 30], 30));       // 2
