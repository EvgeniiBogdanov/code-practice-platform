// Удаление дубликатов из массива через Set
// Напишите функцию getUnique(arr), которая возвращает массив без дубликатов.

const getUnique = (arr) => {
  // Решение тут
};

// Пример вызова:
console.log(getUnique([1, 2, 2, 3, 4, 4, 5])); // [1, 2, 3, 4, 5]
