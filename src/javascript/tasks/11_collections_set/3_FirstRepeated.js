// Напишите функцию firstRepeated(arr), которая возвращает первый элемент, который повторяется в массиве. 
// Если повторов нет — null.

const firstRepeated = (arr) => {
  // Решение тут
};

// Пример:
console.log(firstRepeated([1, 2, 3, 2, 1])); // 2
console.log(firstRepeated([1, 2, 3]));       // null
