// Проверить, есть ли число 7 в массиве, начиная поиск с индекса 3:
const arr = [1, 7, 3, 7, 5];

// Тут код:
const hasElemFrom = () => {};

// Проверка
const result = hasElemFrom(arr, 7, 3);
console.log(result);
