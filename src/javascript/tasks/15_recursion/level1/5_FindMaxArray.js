// Поиск максимума в массиве через рекурсию
// Напишите рекурсивную функцию findMax(arr), находящую максимальное число в массиве.

const findMax = (arr) => {
  // Решение тут
};

// Пример вызова:
console.log(findMax([3, 7, 2, 9, 4])); // 9
