// Прямой отсчет от N до 1 (база рекурсии)
// Напишите рекурсивную функцию countDown(n), которая выводит числа от n до 1.

const countDown = (n) => {
  // Решение тут
};

// Пример вызова:
countDown(3);
// 3
// 2
// 1
