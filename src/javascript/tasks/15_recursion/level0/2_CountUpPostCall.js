// Обратный отсчет (печать на возврате из рекурсии)
// Напишите рекурсивную функцию countUp(n), выводящую числа от 1 до n при возврате.

const countUp = (n) => {
  // Решение тут
};

// Пример вызова:
countUp(3);
// 1
// 2
// 3
