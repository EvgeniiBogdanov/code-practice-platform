// Напишите функцию countUp(n), которая печатает числа от 1 до n,
// но вызов печати должен происходить ПОСЛЕ рекурсивного вызова.
// Вход → Выход
// countUp(3) → 1, 2, 3

const countUp = (n) => {
  // Ваш код здесь
};

countUp(3);
