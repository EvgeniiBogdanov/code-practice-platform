// Рекурсивно просуммировать все цифры во вложенном объекте
// Вывод: -5

const obj = {
  a: 1,
  b: {
    c: 3,
    d: -10,
    e: {
      f: {
        g: 1,
      },
    },
  },
};

const sumNumbers = (data) => {
  // Ваш код здесь
};

console.log(sumNumbers(obj));
