// Напишите функцию deepSum(obj), которая суммирует все числовые значения
// в объекте любой вложенности.
// Вход → Выход
// { a: 1, b: { c: 2, d: { e: 3 } }, f: [4, 5] } → 15

const deepSum = (obj) => {
  // Ваш код здесь
};

console.log(deepSum({ a: 1, b: { c: 2, d: { e: 3 } }, f: [4, 5] }));
