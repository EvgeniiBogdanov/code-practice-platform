// Рекурсивная сумма чисел во вложенном объекте
// Напишите функцию deepSum(obj), суммирующую все числа на всех уровнях вложенности объекта.

const deepSum = (obj) => {
  // Решение тут
};

// Пример вызова:
console.log(deepSum({ a: 1, b: { c: 2, d: { e: 3 } }, f: [4, 5] })); // 15
