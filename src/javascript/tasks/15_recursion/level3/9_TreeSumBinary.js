// Дано бинарное дерево { value, left, right }.
// Напишите функцию treeSum(node), суммирующую все значения узлов.

const tree = {
  value: 1,
  left: { value: 2, left: null, right: { value: 4, left: null, right: null } },
  right: { value: 3, left: null, right: null },
};

const treeSum = (node) => {
  // Ваш код здесь
};

console.log(treeSum(tree)); // 10
