// Напишите функцию flatten(arr), которая разворачивает вложенные массивы
// любой глубины без использования Array.prototype.flat.
// Вход → Выход
// [1, [2, [3, [4, 5]], 6]] → [1, 2, 3, 4, 5, 6]

const flatten = (arr) => {
  // Ваш код здесь
};

console.log(flatten([1, [2, [3, [4, 5]], 6]]));
