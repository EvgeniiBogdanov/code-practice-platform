// Глубокое выпрямление массива (Deep Flatten)
// Напишите функцию flatten(arr), рекурсивно выпрямляющую массив любой вложенности.

const flatten = (arr) => {
  // Решение тут
};

// Пример вызова:
console.log(flatten([1, [2, [3, [4, 5]], 6]])); // [1, 2, 3, 4, 5, 6]
