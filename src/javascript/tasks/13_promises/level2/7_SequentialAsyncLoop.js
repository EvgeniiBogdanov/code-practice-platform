// Последовательное выполнение промисов в цикле
// Напишите async функцию processSequentially(items), которая обрабатывает элементы строго по очереди с задержкой 100 мс.

const processSequentially = async (items) => {
  // Решение тут
};

// Пример вызова:
processSequentially(["первый", "второй", "третий"]).then(() => console.log("Готово!"));
