// Функция loadData может отклонить промис.
// Оберните вызов в try/catch и в случае ошибки
// верните строку "Не удалось загрузить: <причина>"

const loadData = () => Promise.reject("сервер недоступен");

async function safeLoad() {
  // Ваш код здесь
}
