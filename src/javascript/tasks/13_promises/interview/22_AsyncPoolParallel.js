/** Реализуйте функцию asyncPool(count, tasks), которая выполняет массив
 * асинхронных задач (функций, возвращающих Promise) с ограничением
 * количества одновременно выполняющихся задач.
 *
 * Требования:
 * - count — максимальное число задач, выполняющихся параллельно
 * - tasks — массив функций без аргументов, каждая из которых при вызове
 *   возвращает Promise (сама задача запускается только в момент вызова)
 * - Результат должен быть массивом значений в том же порядке
 */
async function asyncPool(count, tasks) {
  // Ваш код здесь
}

const sleep = (ms, v) => () => new Promise((r) => setTimeout(() => r(v), ms));
const tasks = [sleep(50, 'a'), sleep(10, 'b'), sleep(20, 'c')];

asyncPool(2, tasks).then(console.log); // ['a', 'b', 'c']
