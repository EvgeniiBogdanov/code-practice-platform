// Кастомная реализация Promise.all
// Напишите функцию promiseAll(promises), возвращающую массив результатов при успехе.

const promiseAll = (promises) => {
  // Решение тут
};

// Пример вызова:
promiseAll([
  Promise.resolve(1),
  Promise.resolve(2),
  3,
]).then(console.log); // [1, 2, 3]
