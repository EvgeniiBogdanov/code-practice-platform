// Напишите функцию delay(ms), которая возвращает Promise,
// разрешающийся через ms миллисекунд со значением "done".

const delay = (ms) => {
  // Ваш код здесь
};

delay(1000).then(console.log); // через 1 сек выведет "done"
