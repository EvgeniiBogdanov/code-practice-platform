// Дана функция getUser(), возвращающая Promise с объектом пользователя.
// Постройте цепочку: получить пользователя → достать его id →
// вывести "User id: <id>"

const getUser = () => Promise.resolve({ id: 42, name: "Alex" });

// Ваш код здесь
