// Цепочка then для извлечения свойств
// Постройте цепочку промисов для получения user id в формате "User id: <id>".

const getUser = () => Promise.resolve({ id: 42, name: "Alex" });

// Решение тут

// Пример вызова:
// getUser() -> "User id: 42"
