// Оставить числа в диапазоне от 20 до 50
// Напишите функцию filterRange(numbers, min, max), которая возвращает числа в диапазоне от min до max включительно.

const filterRange = (numbers, min, max) => {
  // Решение тут
};

// Пример вызова:
console.log(filterRange([10, 25, 30, 55, 40, 5], 20, 50)); // [25, 30, 40]
