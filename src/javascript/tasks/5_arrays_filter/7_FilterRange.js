// Оставить числа в диапазоне от 20 до 50:
const numbers = [10, 25, 40, 60, 5, 70];

// Тут код:
const filterRange = () => {};

// Проверка
const result = filterRange(numbers, 20, 50);
console.log(result);
