/** Реализуйте компонент Password — поле ввода пароля с возможностью
 * временно показать введённый текст.
 *
 * Требования:
 * - По умолчанию поле имеет type="password" (текст скрыт)
 * - Кнопка переключает видимость пароля: "Показать пароль" / "Скрыть пароль"
 * - Когда пароль показан (isVisible === true), должен запускаться таймер
 *   на hideTimeoutMs миллисекунд (по умолчанию 5000), по истечении
 *   которого пароль автоматически скрывается обратно
 * - Если пользователь вручную нажимает "Скрыть пароль" до истечения
 *   таймера — таймер должен отменяться
 * - Если пользователь продолжает менять текст пароля, пока он видим —
 *   таймер должен перезапускаться (не скрывать раньше времени)
 * - При размонтировании компонента таймер должен корректно очищаться,
 *   чтобы избежать утечек и попыток обновить состояние
 */
import React, { useState } from 'react';

const Password = ({ hideTimeoutMs = 5000 }) => {
  // Ваш код здесь
};

export default Password;

// Пример использования:
// <Password hideTimeoutMs={3000} />