import React, { useState } from 'react';

/**
 * Задача: Поле ввода пароля с авто-скрытием (Password)
 *
 * Реализуйте компонент ввода пароля с возможностью временного показа текста:
 * - По умолчанию поле имеет type="password" (текст скрыт).
 * - Кнопка переключает видимость пароля: "Показать пароль" / "Скрыть пароль".
 * - Когда пароль отображается, запускается таймер на hideTimeoutMs (по умолчанию 5000 мс), после чего пароль автоматически скрывается.
 * - При повторном клике на "Скрыть пароль" до срабатывания таймера таймер отменяется.
 * - При изменении текста пароля во время отображения таймер перезапускается.
 * - При размонтировании компонента таймер должен корректно очищаться.
 */

const Password = ({ hideTimeoutMs = 5000 }) => {
  // Ваш код здесь
};

export default Password;