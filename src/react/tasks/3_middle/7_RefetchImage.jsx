import { useEffect, useRef, useState } from 'react';

/** Реализуйте компонент RefetchImage — карточку изображения с кнопкой
 * "Обновить", которая заново загружает картинку через fetch.
 *
 * Требования:
 * - Изначально показывается изображение по src, переданному в пропсах
 * - При клике на кнопку "Обновить" изображение перезагружается через fetch(src)
 * - Пока идёт загрузка — вместо картинки показывается текст "Загрузка...",
 *   а кнопка блокируется (disabled)
 * - Если запрос завершился с ошибкой (res.ok === false или fetch упал) —
 *   показывается текст "Ошибка" вместо изображения
 * - Если запрос успешен — ответ преобразуется в Blob, создаётся object URL
 *   через URL.createObjectURL, и именно он используется как src картинки
 * - Предыдущий object URL (если он был создан ранее) должен освобождаться
 *   через URL.revokeObjectURL, чтобы избежать утечек памяти
 */

export const RefetchImage = ({ src, alt }) => {
  // Ваш код здесь
};

// Пример использования для проверки в песочнице:
export default function App() {
  return <RefetchImage src="https://picsum.photos/300/200" alt="Пример изображения" />;
}