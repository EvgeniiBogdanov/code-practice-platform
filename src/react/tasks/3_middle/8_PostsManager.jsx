import React, { useEffect, useState } from 'react';

/** Реализуйте компонент PostsManager — менеджер постов, который загружает
 * список постов по переданному URL и позволяет управлять им локально.
 *
 * Требования:
 * - При монтировании (и при изменении url) загружать посты через fetch(url)
 * - Из полученных данных использовать только первые 5 постов
 * - Пока идёт загрузка — показывать текст "Загрузка постов..."
 * - Если запрос завершился с ошибкой (response.ok === false или fetch упал) —
 *   показывается текст "Ошибка: <сообщение>"
 * - Форма с полем ввода и кнопкой "Добавить" позволяет добавить новый пост
 *   локально (без запроса на сервер): пост добавляется в начало списка,
 *   получает уникальный id и помечается как локальный (isLocal: true)
 * - Пустой заголовок (или состоящий из пробелов) добавлять нельзя
 * - После добавления поле ввода должно очищаться
 * - Каждый пост в списке отображается с заголовком (для локальных постов
 *   рядом добавляется пометка "(локальный)") и кнопкой "Удалить"
 * - Кнопка "Удалить" удаляет пост из списка локально (без запроса на сервер)
 * - Если список постов пуст — показывать текст "Список пуст"
 */

export const PostsManager = ({ url }) => {
  // Ваш код здесь
};

export default function App() {
  return <PostsManager url="https://jsonplaceholder.typicode.com/posts" />;
}