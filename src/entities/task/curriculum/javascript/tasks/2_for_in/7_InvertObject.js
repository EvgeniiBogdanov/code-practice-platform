// Инверсия ключей и значений объекта
// Напишите функцию invertObject(obj), которая принимает объект и возвращает новый объект, в котором ключи и значения поменяны местами.

const invertObject = (obj) => {
  // Решение тут
};

// Пример вызова:
const roles = {
  admin: "1",
  editor: "2",
  viewer: "3",
};

console.log(invertObject(roles));
// { "1": "admin", "2": "editor", "3": "viewer" }
