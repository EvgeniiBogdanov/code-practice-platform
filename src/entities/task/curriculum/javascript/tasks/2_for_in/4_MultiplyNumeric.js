// Умножение числовых свойств на 2
// Напишите функцию multiplyNumeric(obj), которая умножает все числовые свойства переданного объекта на 2. Нечисловые свойства остаются без изменений. Функция возвращает исходный объект.

const multiplyNumeric = (obj) => {
  // Решение тут
};

// Пример вызова:
const menu = {
  width: 200,
  height: 300,
  title: "My menu",
  isDefault: true,
};

console.log(multiplyNumeric(menu));
// { width: 400, height: 600, title: "My menu", isDefault: true }
