// Умножение числовых свойств на 2
// Напишите функцию multiplyNumeric(obj), которая перебирает переданный объект и умножает все его числовые свойства на 2. Нечисловые свойства должны остаться без изменений. Функция возвращает исходный объект.

const multiplyNumeric = (obj) => {
  // Решение тут
};

// Пример вызова:
const menu = {
  width: 200,
  height: 300,
  title: "My menu",
  isDefault: true,
};

console.log(multiplyNumeric(menu));
// { width: 400, height: 600, title: "My menu", isDefault: true }
