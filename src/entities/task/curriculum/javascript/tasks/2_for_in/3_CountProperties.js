// Подсчет количества свойств объекта
// Напишите функцию countProps(obj), которая подсчитывает и возвращает количество свойств переданного объекта с помощью цикла for...in.

const countProps = (obj) => {
  // Решение тут
};

// Пример вызова:
console.log(countProps({ a: 1, b: 2, c: 3 })); // 3
console.log(countProps({ name: "Alice" }));     // 1
console.log(countProps({}));                    // 0
