// Проверка объекта на пустоту (isEmpty)
// Напишите функцию isEmpty(obj), которая возвращает true, если у объекта нет собственных свойств, и false, если есть хотя бы одно.

const isEmpty = (obj) => {
  // Решение тут
};

// Пример вызова:
console.log(isEmpty({}));              // true
console.log(isEmpty({ name: "Ann" })); // false
console.log(isEmpty({ 0: "zero" }));   // false
