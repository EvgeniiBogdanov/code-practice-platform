// Сумма зарплат в объекте
// Напишите функцию sumSalaries(salaries), которая принимает объект с числовыми зарплатами и возвращает их общую сумму. Если объект пуст, функция возвращает 0.

const sumSalaries = (salaries) => {
  // Решение тут
};

// Пример вызова:
const salaries = {
  John: 1000,
  Ann: 1600,
  Pete: 1300,
};

console.log(sumSalaries(salaries)); // 3900
console.log(sumSalaries({}));       // 0
