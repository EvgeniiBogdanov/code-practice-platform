// Сумма зарплат в объекте
// Напишите функцию sumSalaries(salaries), которая принимает объект с зарплатами и возвращает их общую сумму с помощью цикла for...in. Если объект пуст, функция должна возвращать 0.

const sumSalaries = (salaries) => {
  // Решение тут
};

// Пример вызова:
const salaries = {
  John: 1000,
  Ann: 1600,
  Pete: 1300,
};

console.log(sumSalaries(salaries)); // 3900
console.log(sumSalaries({}));         // 0
