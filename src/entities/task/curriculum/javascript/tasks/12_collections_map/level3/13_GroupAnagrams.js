// Группировка анаграмм с помощью Map
// Напишите функцию groupAnagrams(words), которая группирует анаграммы в массивы.

const groupAnagrams = (words) => {
  // Решение тут
};

// Пример вызова:
console.log(groupAnagrams(["eat", "tea", "tan", "ate", "nat", "bat"]));
// [["eat", "tea", "ate"], ["tan", "nat"], ["bat"]]
