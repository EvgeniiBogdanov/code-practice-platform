// Группировка объектов в Map
// Напишите функцию groupUsersByRole(users), группирующую пользователей по роли.

const groupUsersByRole = (users) => {
  // Решение тут
};

// Пример вызова:
const users = [
  { name: "Alice", role: "admin" },
  { name: "Bob", role: "user" },
  { name: "Charlie", role: "admin" },
];
console.log(groupUsersByRole(users));
