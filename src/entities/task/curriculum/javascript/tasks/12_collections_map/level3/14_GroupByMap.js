// Универсальная группировка по функции-селектору
// Напишите функцию groupBy(items, keySelector), которая группирует массив элементов по ключу, возвращаемому функцией keySelector.

const groupBy = (items, keySelector) => {
  // Решение тут
};

// Пример вызова:
console.log(groupBy([6.1, 4.2, 6.3], Math.floor));
// Map(2) { 6 => [6.1, 6.3], 4 => [4.2] }
