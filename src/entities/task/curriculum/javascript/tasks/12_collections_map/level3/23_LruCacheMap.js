// Реализация LRU Cache
// Реализуйте класс LRUCache(capacity) со следующими методами:
// 1. get(key) — возвращает значение ключа, если он присутствует в кэше, иначе undefined (или -1). Обращение к ключу обновляет его приоритет использования (делает самым свежим).
// 2. put(key, value) — добавляет или обновляет значение ключа. Если количество элементов превышает лимит capacity, вытесняется наименее используемый (самый старый) элемент.
// 3. Все операции должны выполняться за время O(1).

class LRUCache {
  // Решение тут
}

// Пример вызова:
const cache = new LRUCache(2);
cache.put("a", 1);
cache.put("b", 2);
console.log(cache.get("a")); // 1

cache.put("c", 3); // Емкость превышена: 'b' удаляется
console.log(cache.get("b")); // undefined
console.log(cache.get("c")); // 3
console.log(cache.get("a")); // 1
