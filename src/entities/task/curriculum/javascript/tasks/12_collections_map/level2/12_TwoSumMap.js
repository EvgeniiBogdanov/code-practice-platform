// Двусумма (Two Sum) за время O(N)
// Напишите функцию twoSum(nums, target), которая находит индексы двух чисел в массиве, дающих в сумме target.
// Временная сложность должна быть O(N).

const twoSum = (nums, target) => {
  // Решение тут
};

// Пример вызова:
console.log(twoSum([2, 7, 11, 15], 9)); // [0, 1]
console.log(twoSum([3, 2, 4], 6));       // [1, 2]
