// Группировка пользователей по городам
// Напишите функцию groupByCity(users), которая возвращает Map, где ключ — город, а значение — массив пользователей из этого города.

const users = [
  { name: "Иван", city: "Москва" },
  { name: "Анна", city: "СПб" },
  { name: "Петр", city: "Москва" },
];

const groupByCity = (users) => {
  // Решение тут
};

// Пример вызова:
console.log(groupByCity(users));
