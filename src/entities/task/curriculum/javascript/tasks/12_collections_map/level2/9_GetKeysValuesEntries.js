// Извлечение ключей и значений Map в массивы
// Напишите функцию getMapData(map), которая возвращает объект { keys: [...], values: [...] }.

const getMapData = (map) => {
  // Решение тут
};

// Пример вызова:
const map = new Map([["x", 1], ["y", 2]]);
console.log(getMapData(map)); // { keys: ["x", "y"], values: [1, 2] }
