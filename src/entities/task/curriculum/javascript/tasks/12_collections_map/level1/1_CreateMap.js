// Создание и наполнение Map
// Напишите функцию createFruitMap(), которая создает и возвращает Map с ключами: "яблоко" -> 10, "банан" -> 20, "апельсин" -> 15.

const createFruitMap = () => {
  // Решение тут
};

// Пример вызова:
console.log(createFruitMap().get("банан")); // 20
