// Частотный анализ слов
// Напишите функцию wordFrequency(words), которая принимает массив слов и возвращает Map с частотой появления каждого слова.

const wordFrequency = (words) => {
  // Решение тут
};

// Пример вызова:
console.log(wordFrequency(["apple", "banana", "apple", "orange", "banana", "apple"]));
// Map(3) { 'apple' => 3, 'banana' => 2, 'orange' => 1 }
