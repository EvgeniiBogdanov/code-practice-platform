// Инициализация Map из массива пар
// Напишите функцию initMap(pairs), которая принимает массив пар [ключ, значение] и возвращает инициализированный Map.

const initMap = (pairs) => {
  // Решение тут
};

// Пример вызова:
console.log(initMap([["a", 1], ["b", 2]]).size); // 2
