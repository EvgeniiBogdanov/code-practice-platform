// Полифил Promise.all
// Реализуйте функцию promiseAll(promises), работающую аналогично Promise.all.

const promiseAll = (promises) => {
  // Решение тут
};

// Пример вызова:
promiseAll([
  Promise.resolve(1),
  Promise.resolve(2),
  3,
]).then(console.log); // [1, 2, 3]
