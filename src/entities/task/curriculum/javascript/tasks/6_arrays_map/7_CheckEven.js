// Проверка чётности чисел массива
// Напишите функцию checkEven(numbers), которая возвращает массив булевых значений (true для четных, false для нечетных).

const checkEven = (numbers) => {
  // Решение тут
};

// Пример вызова:
console.log(checkEven([1, 2, 3, 4, 5])); // [false, true, false, true, false]
