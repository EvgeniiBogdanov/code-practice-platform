// Извлечение имен из массива объектов
// Напишите функцию getNames(users), которая возвращает массив имен пользователей.

const users = [
  { name: "Анна", age: 25 },
  { name: "Иван", age: 30 },
  { name: "Мария", age: 20 },
];

const getNames = (users) => {
  // Решение тут
};

// Пример вызова:
console.log(getNames(users)); // ["Анна", "Иван", "Мария"]
