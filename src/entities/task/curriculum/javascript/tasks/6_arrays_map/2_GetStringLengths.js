// Длины строк в массиве
// Напишите функцию getStringLengths(words), которая возвращает массив длин каждой строки.

const getStringLengths = (words) => {
  // Решение тут
};

// Пример вызова:
console.log(getStringLengths(["apple", "banana", "cherry"])); // [5, 6, 6]
