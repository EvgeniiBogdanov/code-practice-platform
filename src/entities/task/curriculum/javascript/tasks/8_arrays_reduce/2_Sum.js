// Сумма элементов числового массива
// Напишите функцию sum(arr), которая возвращает сумму всех элементов массива.
// Для пустого массива возвращает 0.

const sum = (arr) => {
  // Решение тут
};

// Пример вызова:
console.log(sum([1, 2, 3, 4, 5])); // 15
console.log(sum([10, -5, 3]));      // 8
console.log(sum([]));               // 0
