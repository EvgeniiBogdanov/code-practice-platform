// Поиск минимума и максимума за один проход
// Напишите функцию findMinMax(numbers), которая находит минимальное и максимальное число в массиве за один проход и возвращает { min, max }.

const findMinMax = (numbers) => {
  // Решение тут
};

// Пример вызова:
console.log(findMinMax([23, 4, 45, 2, 8, 16])); // { min: 2, max: 45 }
