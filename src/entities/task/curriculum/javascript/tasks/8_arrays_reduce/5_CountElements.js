// Подсчёт количества элементов
// Напишите функцию countOccurrences(arr), которая возвращает объект,
// где ключи — элементы массива, а значения — количество их вхождений.

const countOccurrences = (arr) => {
  // Решение тут
};

// Пример вызова:
console.log(countOccurrences(["a", "b", "a", "c", "b", "a"])); // { a: 3, b: 2, c: 1 }
console.log(countOccurrences([1, 2, 1, 1]));                   // { 1: 3, 2: 1 }
