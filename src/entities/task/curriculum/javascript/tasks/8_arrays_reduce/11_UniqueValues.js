// Получение уникальных значений массива
// Напишите функцию unique(arr), которая возвращает массив только с уникальными значениями исходного массива.

const unique = (arr) => {
  // Решение тут
};

// Пример вызова:
console.log(unique([1, 2, 2, 3, 4, 4, 5, 1])); // [1, 2, 3, 4, 5]
