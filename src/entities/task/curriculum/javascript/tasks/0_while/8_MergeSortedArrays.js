// Слияние двух отсортированных массивов
// Напишите функцию merge(arr1, arr2), которая принимает два отсортированных по возрастанию
// массива и возвращает новый объединенный отсортированный массив за O(N + M).

const merge = (arr1, arr2) => {
  // Решение тут
};

// Пример вызова:
console.log(merge([1, 3, 5], [2, 4, 6])); // [1, 2, 3, 4, 5, 6]
console.log(merge([1, 2], [3, 4, 5, 6])); // [1, 2, 3, 4, 5, 6]
console.log(merge([], [1, 2]));           // [1, 2]
