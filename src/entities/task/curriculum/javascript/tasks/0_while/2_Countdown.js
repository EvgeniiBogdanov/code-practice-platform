// Обратный отсчёт от N до 1
// Напишите функцию countdown(n), которая принимает положительное число n
// и выводит в консоль числа от n до 1 с помощью цикла while.

const countdown = (n) => {
  // Решение тут
};

// Пример вызова:
countdown(3);
// 3
// 2
// 1
