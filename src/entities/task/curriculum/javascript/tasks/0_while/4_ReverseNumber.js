// Разворот числа
// Напишите функцию reverseNumber(num), которая принимает положительное целое число num
// и возвращает число с обратным порядком цифр.

const reverseNumber = (num) => {
  // Решение тут
};

// Пример вызова:
console.log(reverseNumber(12345)); // 54321
console.log(reverseNumber(980));   // 89
console.log(reverseNumber(7));     // 7
