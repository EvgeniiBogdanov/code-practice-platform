// Нормализация объекта через деструктуризацию
// Напишите функцию extractUserInfo(user), извлекающую name, email (по умолчанию 'no-email') и собирающую остальные поля в объект extra.

const extractUserInfo = (user) => {
  // Решение тут
};

// Пример вызова:
console.log(extractUserInfo({ name: "Bob", city: "Moscow", age: 30 }));
// { name: 'Bob', email: 'no-email', extra: { city: 'Moscow', age: 30 } }
