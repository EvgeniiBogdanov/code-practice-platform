// Преобразование объекта в массивы ключей, значений и записей
// Напишите функцию inspectObject(obj), возвращающую { keys: [...], values: [...], entries: [...] }.

const inspectObject = (obj) => {
  // Решение тут
};

// Пример вызова:
console.log(inspectObject({ a: 1, b: 2 }));
// { keys: ['a', 'b'], values: [1, 2], entries: [['a', 1], ['b', 2]] }
