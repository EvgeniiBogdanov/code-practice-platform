// Инверсия ключей и значений объекта (invert)
// Напишите функцию invert(obj), которая принимает объект и возвращает новый объект, в котором ключи и значения поменяны местами.

const invert = (obj) => {
  // Решение тут
};

// Пример вызова:
console.log(invert({ a: "1", b: "2", c: "3" })); // { '1': 'a', '2': 'b', '3': 'c' }
console.log(invert({ x: 10, y: 20 }));            // { '10': 'x', '20': 'y' }
