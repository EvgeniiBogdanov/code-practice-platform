// Глубокое слияние объектов (deepMerge)
// Напишите функцию deepMerge(target, ...sources), которая объединяет несколько объектов в единую глубокую структуру.
//
// Требования:
// 1. Вложенные объекты с одинаковыми ключами объединяются рекурсивно.
// 2. Примитивы и массивы из последующих источников перезаписывают предыдущие значения.
// 3. Защита от Prototype Pollution: запрещено перезаписывать '__proto__', 'constructor', 'prototype'.

const deepMerge = (target, ...sources) => {
  // Решение тут
};

// Пример вызова:
const obj1 = { a: 1, b: { x: 10, y: 20 } };
const obj2 = { b: { y: 99, z: 30 }, c: 3 };
console.log(deepMerge({}, obj1, obj2));
// { a: 1, b: { x: 10, y: 99, z: 30 }, c: 3 }
