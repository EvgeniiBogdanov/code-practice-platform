// Базовые операции со свойствами объекта (CRUD)
// Напишите функцию manageUser(), создающую объект user, добавляющую поле role: 'admin', изменяющую age и удаляющую поле temp.

const manageUser = () => {
  // Решение тут
};

// Пример вызова:
console.log(manageUser());
