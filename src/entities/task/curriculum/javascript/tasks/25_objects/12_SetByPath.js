// Установка значения по глубокому пути (set / _.set)
// Напишите функцию set(obj, path, value), которая устанавливает значение свойства по указанному пути, создавая недостающие промежуточные объекты или массивы.
//
// Требования:
// 1. Путь path может быть строкой ('a.b.c', 'users[0].name') или массивом (['a', 'b', 'c']).
// 2. Если следующий ключ — целое число, создается пустой массив [], иначе пустой объект {}.
// 3. Функция мутирует исходный объект obj и возвращает его.
// 4. Защита от Prototype Pollution: запрещено модифицировать свойства '__proto__', 'prototype', 'constructor'.

const set = (obj, path, value) => {
  // Решение тут
};

// Пример вызова:
const obj1 = {};
set(obj1, "a.b.c", 42);
console.log(JSON.stringify(obj1)); // {"a":{"b":{"c":42}}}

const obj2 = {};
set(obj2, "users[0].name", "Alice");
console.log(JSON.stringify(obj2)); // {"users":[{"name":"Alice"}]}
