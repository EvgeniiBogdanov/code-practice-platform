// Реализация функции debounce
// Напишите функцию debounce(fn, ms), которая откладывает вызов fn до тех пор, пока с момента последнего вызова не пройдет ms миллисекунд.

const debounce = (fn, ms) => {
  // Решение тут
};

// Пример вызова:
const log = debounce((msg) => console.log(msg), 300);
log("a");
log("b");
log("c"); // Выведет "c" через 300 мс
