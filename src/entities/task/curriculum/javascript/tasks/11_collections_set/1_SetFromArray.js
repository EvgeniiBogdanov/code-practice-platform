// Удаление дубликатов из массива
// Напишите функцию unique(arr), которая возвращает массив только с уникальными значениями.

const unique = (arr) => {
  // Решение тут
};

// Пример вызова:
console.log(unique([1, 2, 2, 3, 4, 4, 5])); // [1, 2, 3, 4, 5]
console.log(unique(["a", "b", "a", "c"]));   // ["a", "b", "c"]
