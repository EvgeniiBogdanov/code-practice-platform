// Поиск первого повторяющегося элемента
// Напишите функцию firstRepeated(arr), которая возвращает первый элемент, встречающийся в массиве более одного раза. Если дубликатов нет, возвращает undefined.

const firstRepeated = (arr) => {
  // Решение тут
};

// Пример вызова:
console.log(firstRepeated([2, 5, 1, 2, 3, 5, 1])); // 2
console.log(firstRepeated([2, 1, 3, 5, 3, 2]));    // 3
console.log(firstRepeated([1, 2, 3, 4]));          // undefined
