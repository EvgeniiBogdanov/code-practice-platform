// Проверка строки на палиндром
// Напишите функцию isPalindrome(str), которая возвращает true, если строка читается
// одинаково слева направо и справа налево (без учета регистра), иначе false.

const isPalindrome = (str) => {
  // Решение тут
};

// Пример вызова:
console.log(isPalindrome("радар")); // true
console.log(isPalindrome("Anna"));  // true
console.log(isPalindrome("hello")); // false
