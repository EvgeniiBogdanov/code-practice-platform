// Сумма чисел от 1 до N
// Напишите функцию sumTo(n), которая возвращает сумму всех целых чисел от 1 до n.

const sumTo = (n) => {
  // Решение тут
};

// Пример вызова:
console.log(sumTo(4));   // 10
console.log(sumTo(100)); // 5050
