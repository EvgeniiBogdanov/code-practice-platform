// Декоратор однократного вызова (once)
// Напишите функцию-декоратор once(fn), которая гарантирует, что переданная функция fn будет выполнена только один раз при первом вызове. При последующих вызовах возвращается сохраненный результат первого вызова.

const once = (fn) => {
  // Решение тут
};

// Пример вызова:
let callCount = 0;
const pay = once((amount) => {
  callCount++;
  return `Оплачено: ${amount} руб. (транзакция #${callCount})`;
});

console.log(pay(500)); // 'Оплачено: 500 руб. (транзакция #1)'
console.log(pay(1000)); // 'Оплачено: 500 руб. (транзакция #1)'
console.log(callCount); // 1

