// Создание промиса с задержкой
// Напишите функцию delay(ms, value), которая возвращает промис, разрешающийся значением value через ms миллисекунд.

const delay = (ms, value) => {
  // Решение тут
};

// Пример вызова:
delay(500, "Готово").then(console.log); // "Готово" через 500 мс
