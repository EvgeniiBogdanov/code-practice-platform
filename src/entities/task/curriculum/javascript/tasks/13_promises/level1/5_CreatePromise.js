// Создание промиса с задержкой
// Напишите функцию delay(ms), которая возвращает промис, разрешающийся строкой "done" через ms миллисекунд.

const delay = (ms) => {
  // Решение тут
};

// Пример вызова:
delay(1000).then(console.log);
