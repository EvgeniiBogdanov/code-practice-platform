// Создание базового промиса с задержкой
// Напишите функцию delay(ms), возвращающую Promise с результатом "done".

const delay = (ms) => {
  // Решение тут
};

// Пример вызова:
delay(1000).then(console.log); // "done"
