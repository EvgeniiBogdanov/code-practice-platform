// Возврат значения из async функции
// Напишите async функцию getUser(id), которая возвращает объект { id, name: "Пользователь " + id }.

const getUser = async (id) => {
  // Решение тут
};

// Пример вызова:
getUser(42).then((user) => console.log(user)); // { id: 42, name: "Пользователь 42" }
