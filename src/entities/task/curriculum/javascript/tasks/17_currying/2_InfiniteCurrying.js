// Бесконечное каррирование функции суммирования
// Напишите функцию curry_sum, позволяющую суммировать произвольное количество вызовов до терминального вызова без аргументов ().

function curry_sum(x) {
  // Решение тут
}

// Пример вызова:
console.log(curry_sum(1)()); // 1
console.log(curry_sum(1)(2)(3)()); // 6

