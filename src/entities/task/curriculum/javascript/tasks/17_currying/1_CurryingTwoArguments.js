// Каррирование функции сложения двух чисел
// Напишите функцию add(a), которая возвращает функцию, принимающую b и возвращающую их сумму: add(a)(b).

const add = (a) => {
  // Решение тут
};

// Пример вызова:
console.log(add(2)(3)); // 5
console.log(add(10)(5)); // 15
