// Каррирование суммы с двумя аргументами
// Реализуйте функцию sum, поддерживающую как прямой вызов sum(1, 2), так и каррированный вызов sum(1)(2).

const sum = (val1, val2) => {
  // Решение тут
};

// Пример вызова:
console.log(sum(1, 2)); // 3
console.log(sum(1)(2)); // 3

