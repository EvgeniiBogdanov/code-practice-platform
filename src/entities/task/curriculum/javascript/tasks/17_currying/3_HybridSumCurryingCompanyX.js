// Универсальная функция сложения (гибридный вызов)
// Напишите функцию sum, которая может вызываться как sum(1, 2), так и sum(1)(2), возвращая сумму чисел.

function sum(...args) {
  // Решение тут
}

// Пример вызова:
console.log(sum(2, 3)); // 5
console.log(sum(2)(3)); // 5
