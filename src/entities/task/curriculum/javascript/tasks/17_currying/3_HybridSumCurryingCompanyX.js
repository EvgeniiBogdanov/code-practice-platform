// Каррирование с приведением к числу (Symbol.toPrimitive / valueOf)
// Напишите функцию sum(a), поддерживающую цепочку вызовов любой длины вида sum(1)(2)(3)...
// При приведении к числу возвращает накопленную сумму всех переданных аргументов.

function sum(a) {
  // Решение тут
}

// Пример вызова:
console.log(Number(sum(1)(2)(3))); // 6
console.log(+sum(2)(4));           // 6

