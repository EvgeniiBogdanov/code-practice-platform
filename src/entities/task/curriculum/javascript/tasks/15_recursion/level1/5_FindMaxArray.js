// Поиск максимального числа в массиве
// Напишите функцию findMax(arr), которая находит максимальное число в числовом массиве.

const findMax = (arr) => {
  // Решение тут
};

// Пример вызова:
console.log(findMax([3, 7, 2, 9, 4])); // 9
