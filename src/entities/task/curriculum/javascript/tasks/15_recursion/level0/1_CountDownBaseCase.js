// Обратный отсчет от N до 1
// Напишите функцию countDown(n), которая выводит в консоль числа от n до 1 в порядке убывания.

const countDown = (n) => {
  // Решение тут
};

// Пример вызова:
countDown(3);
// 3
// 2
// 1
