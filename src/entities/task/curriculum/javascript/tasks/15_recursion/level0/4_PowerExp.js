// Возведение числа в степень
// Напишите функцию power(base, exp), которая возводит число base в неотрицательную степень exp.

const power = (base, exp) => {
  // Решение тут
};

// Пример вызова:
console.log(power(2, 5)); // 32
console.log(power(3, 3)); // 27
