// Глубокое выпрямление массива (Deep Flatten)
// Напишите функцию flatten(arr), которая выпрямляет многомерный массив любой глубины вложенности в одномерный.

const flatten = (arr) => {
  // Решение тут
};

// Пример вызова:
console.log(flatten([1, [2, [3, [4, 5]], 6]])); // [1, 2, 3, 4, 5, 6]
