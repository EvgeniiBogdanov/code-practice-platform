// Сумма значений узлов бинарного дерева
// Напишите функцию treeSum(node), возвращающую сумму всех значений в бинарном дереве.

const tree = {
  value: 1,
  left: { value: 2, left: null, right: { value: 4, left: null, right: null } },
  right: { value: 3, left: null, right: null },
};

const treeSum = (node) => {
  // Решение тут
};

// Пример вызова:
console.log(treeSum(tree)); // 10
