// Максимальная глубина бинарного дерева (Tree Depth)
// Напишите функцию treeDepth(node), возвращающую максимальную глубину бинарного дерева.

const tree = {
  value: 1,
  left: { value: 2, left: null, right: { value: 4, left: null, right: null } },
  right: { value: 3, left: null, right: null },
};

const treeDepth = (node) => {
  // Решение тут
};

// Пример вызова:
console.log(treeDepth(tree)); // 3
