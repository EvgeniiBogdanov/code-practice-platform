import React, { useState, useRef } from 'react';

/**
 * Задача: Автокомплит (Combobox) с клавиатурной навигацией
 *
 * Реализуйте компонент AutocompleteCombobox:
 * 1. Фильтрация подсказок:
 *    - При вводе в текстовое поле фильтруйте переданный массив items без учета регистра.
 *    - Если поле ввода пустое, выпадающий список скрыт.
 *    - Если совпадений нет, в выпадающем списке отображается сообщение «Ничего не найдено».
 *
 * 2. Навигация с клавиатуры в поле ввода:
 *    - Стрелка «Вниз» (ArrowDown): переход к следующей подсказке в списке.
 *    - Стрелка «Вверх» (ArrowUp): переход к предыдущей подсказке в списке.
 *    - Клавиша Enter: выбор активной подсказки (значение подставляется в инпут, список закрывается, вызывается onSelect).
 *    - Клавиша Escape: закрытие выпадающего списка.
 *
 * 3. Мышь и управление фокусом:
 *    - Клик по подсказке мышью выбирает её и закрывает список.
 *    - Наведение курсора на подсказку делает её активной.
 *    - При потере фокуса (blur) выпадающий список закрывается без потери возможности клика по подсказке.
 *
 * 4. Семантика и доступность (a11y):
 *    - Семантические атрибуты: role="combobox", aria-expanded, role="listbox", role="option", aria-selected.
 *    - Без использования сторонних библиотек и inline-стилей.
 */

export const DEFAULT_ITEMS = [
  'JavaScript',
  'TypeScript',
  'React',
  'Vue.js',
  'Angular',
  'Node.js',
  'Next.js',
  'GraphQL',
  'Tailwind CSS',
  'PostgreSQL',
];

export default function AutocompleteCombobox({ items = DEFAULT_ITEMS, onSelect }) {
  // Напишите ваш код здесь

  return (
    <div>
      <h3>Поиск технологии</h3>
      {/* Поле ввода инпута с доступностью */}
      {/* Выпадающий список подсказок ul > li */}
    </div>
  );
}
