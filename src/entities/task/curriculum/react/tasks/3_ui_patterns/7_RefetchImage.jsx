import React, { useEffect, useRef, useState } from 'react';

/**
 * Задача: Карточка изображения с повторной загрузкой (RefetchImage)
 *
 * Реализуйте компонент изображения с кнопкой «Обновить»:
 * - Изначально отображается изображение по адресу src, переданному в пропсах.
 * - При клике на кнопку «Обновить» картинка повторно загружается через fetch(src).
 * - На время загрузки отображается текст "Загрузка...", а кнопка становится неактивной (disabled).
 * - При ошибке запроса вместо изображения отображается текст "Ошибка".
 * - При успешной загрузке полученные бинарные данные отображаются как картинка.
 * - Уделите внимание контролю памяти и своевременному освобождению создаваемых ресурсов при повторной загрузке и размонтировании.
 */

export const RefetchImage = ({ src, alt }) => {
  // Ваш код здесь
};

// Пример использования для проверки в песочнице:
export default function App() {
  return <RefetchImage src="https://picsum.photos/300/200" alt="Пример изображения" />;
}