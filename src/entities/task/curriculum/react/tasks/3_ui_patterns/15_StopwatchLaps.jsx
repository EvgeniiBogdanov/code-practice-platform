import React, { useState, useRef, useEffect } from 'react';

/**
 * Задача: Секундомер с фиксацией кругов (Lap Times) и защитой от дрейфа
 *
 * Реализуйте компонент точного секундомера Stopwatch:
 * 1. Форматирование времени:
 *    - Отображайте текущее время в формате MM:SS.ms (минуты:секунды.сотые доли секунды).
 *    - Пример: 02:14.38
 *
 * 2. Точность и компенсация дрейфа (Drift Compensation):
 *    - Не используйте наивный инкремент вида time + 10 в setInterval, так как задержки
 *      Event Loop накапливают системную погрешность и часы начинают отставать.
 *    - Рассчитывайте прошедшее время через разницу временных меток:
 *      текущее время = Date.now() - startTime + accumulatedTime.
 *
 * 3. Кнопки управления:
 *    - «Старт»: запускает отсчет времени.
 *    - «Пауза»: приостанавливает таймер с сохранением накопленного времени.
 *    - «Круг» (Lap): фиксирует текущее время круга в список (доступна только во время работы секундомера).
 *    - «Сброс»: останавливает таймер, обнуляет время и очищает список кругов.
 *
 * 4. Список кругов:
 *    - Для каждого круга отображайте его порядковый номер, общее зафиксированное время
 *      и разницу (сплит) с предыдущим кругом.
 *    - Самый свежий круг отображается первым в списке.
 *
 * 5. Безопасность и утечки памяти:
 *    - Все таймеры должны корректно очищаться при остановке, сбросе и размонтировании компонента.
 *    - Без использования сторонних библиотек и inline-стилей.
 */

export default function Stopwatch() {
  // Напишите ваш код здесь

  return (
    <div>
      <h2>Спортивный секундомер</h2>
      {/* Табло времени MM:SS.ms */}
      {/* Кнопки управления: Старт/Пауза, Круг, Сброс */}
      {/* Список зафиксированных кругов */}
    </div>
  );
}
