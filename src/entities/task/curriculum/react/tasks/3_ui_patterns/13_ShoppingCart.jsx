import React, { useState } from 'react';

/**
 * Задача: Корзина товаров с расчетом скидок и промокодов
 *
 * Реализуйте компонент ShoppingCart:
 * 1. Управление списком товаров:
 *    - Отобразите товары из initialItems (название, цена за единицу, количество, остаток maxStock).
 *    - Кнопка «-»: уменьшает количество (минимальное значение — 1, при 1 кнопка заблокирована).
 *    - Кнопка «+»: увеличивает количество (не может превышать maxStock, при достижении кнопка заблокирована).
 *    - Кнопка «Удалить»: удаляет товар из корзины.
 *    - При пустой корзине отобразите текст «Корзина пуста».
 *
 * 2. Система промокодов:
 *    - Поле ввода и кнопка «Применить».
 *    - «SAVE10»: дает скидку 10% от суммы товаров (subtotal).
 *    - «SALE500»: скидка 500 ₽, применяется только если сумма товаров не менее 2000 ₽.
 *      Если сумма меньше — отобразите ошибку «Минимальная сумма для промокода 2000 ₽».
 *    - При неизвестном промокоде — ошибка «Неверный промокод».
 *    - Примененный промокод можно сбросить кнопкой «Отменить промокод».
 *
 * 3. Расчет итогов (строго вычисляемые значения без синхронизирующих useEffect):
 *    - Стоимость товаров (subtotal)
 *    - Скидка (discount)
 *    - Доставка: бесплатно (0 ₽) при сумме со скидкой от 3000 ₽, иначе 300 ₽.
 *      Если корзина пуста — стоимость доставки 0 ₽.
 *    - Итого к оплате: (subtotal - discount) + delivery.
 */

export const INITIAL_CART_ITEMS = [
  { id: 1, name: 'Беспроводная клавиатура', price: 4500, quantity: 1, maxStock: 4 },
  { id: 2, name: 'Эргономичная мышь', price: 2800, quantity: 2, maxStock: 5 },
  { id: 3, name: 'USB-C хаб', price: 1900, quantity: 1, maxStock: 3 },
];

export default function ShoppingCart({ initialItems = INITIAL_CART_ITEMS }) {
  // Напишите ваш код здесь

  return (
    <div>
      <h2>Корзина заказов</h2>
      {/* Список товаров или сообщение о пустой корзине */}
      {/* Форма промокода */}
      {/* Блок итогового расчета: товары, скидка, доставка, итого */}
    </div>
  );
}
