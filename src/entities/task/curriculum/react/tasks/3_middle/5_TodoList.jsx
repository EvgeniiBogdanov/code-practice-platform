import React, { useState } from 'react';

/**
 * Задача: Список задач (Todo List)
 *
 * Реализуйте компонент списка задач:
 * 1. Добавление задачи: поле ввода и кнопка «Добавить». Пустые задачи добавляться не должны, после добавления инпут очищается.
 * 2. Отображение списка задач: каждая задача содержит id, текст и статус выполнения (completed).
 * 3. Удаление задачи: кнопка «Удалить» рядом с каждой задачей, удаляющая её из списка.
 * 4. Отметка выполнения: клик по тексту задачи переключает статус completed (выполненная задача отображается перечеркнутой).
 */

const TodoList = () => {
  // Напишите ваш код здесь

  return (
    <div>
      {/* Напишите вашу верстку здесь */}
    </div>
  );
};

export default TodoList;