import React, { useEffect, useState } from 'react';

/**
 * Задача: Менеджер постов (PostsManager)
 *
 * Реализуйте компонент загрузки и локального управления списком постов:
 * - При монтировании (и изменении url) загружайте посты через fetch(url).
 * - Отображайте первые 5 постов из полученных данных.
 * - Пока идёт загрузка — показывайте текст "Загрузка постов...".
 * - При ошибке запроса — показывайте текст "Ошибка: <сообщение>".
 * - Форма с полем ввода и кнопкой «Добавить» позволяет добавить новый пост локально (в начало списка с пометкой "(локальный)").
 * - Пустой заголовок добавлять нельзя, после добавления поле ввода очищается.
 * - Кнопка «Удалить» удаляет пост из локального списка.
 * - Если список пуст — показывайте текст "Список пуст".
 */

export const PostsManager = ({ url }) => {
  // Ваш код здесь
};

export default function App() {
  return <PostsManager url="https://jsonplaceholder.typicode.com/posts" />;
}