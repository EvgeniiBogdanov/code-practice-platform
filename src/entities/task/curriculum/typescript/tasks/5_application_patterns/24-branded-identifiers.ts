// В системе есть идентификаторы пользователей и идентификаторы заказов,
// оба являются строками. Сделайте так, чтобы TypeScript не позволял
// случайно передать идентификатор заказа туда, где ожидается
// идентификатор пользователя, и наоборот — даже если оба значения
// на этапе выполнения являются обычными строками.

const getUserById = (id) => {
  // ...
};

const getOrderById = (id) => {
  // ...
};

const userId = "user-1";
const orderId = "order-1";

getUserById(orderId); // это должно быть ошибкой
