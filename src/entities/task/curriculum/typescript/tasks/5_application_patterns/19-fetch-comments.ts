// Напишите и типизируйте функцию, выполняющую запрос за данными
// по переданному URL.
//
// После получения данных выведите их в консоль в формате:
// "ID: id, Email: email".

const COMMENTS_URL = "https://jsonplaceholder.typicode.com/comments";

const getData = (url) => {
  // Ваш код здесь
};

getData(COMMENTS_URL).then((data) => {
  // Ваш код здесь
});
