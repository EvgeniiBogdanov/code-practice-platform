// Опишите тип для пары [имя, возраст], где количество элементов,
// их порядок и типы строго фиксированы.

const entry = ["Alice", 30];

const printEntry = (entry) => {
  console.log(`${entry[0]} - ${entry[1]}`);
};
