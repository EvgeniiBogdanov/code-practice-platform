// Функция должна корректно обрабатывать оба варианта входных данных,
// не вызывая ошибок типизации внутри своего тела.

const formatId = (id) => {
  if (/* условие */) {
    return id.toUpperCase();
  }
  return id.toFixed(2);
};
