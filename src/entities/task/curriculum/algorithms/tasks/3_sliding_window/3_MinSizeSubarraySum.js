// Напишите функцию minSubArrayLen(target, nums), которая принимает
// положительное целое число (target) и массив положительных целых чисел (nums).
// Функция должна найти минимальную длину непрерывного подмассива,
// сумма элементов которого больше или равна target.
// Если такого подмассива нет, функция должна вернуть 0.
//
// Подмассив — это непрерывная последовательность элементов внутри массива.
//
// Примеры:
// minSubArrayLen(7, [2, 3, 1, 2, 4, 3]) -> 2 (подмассив [4, 3])
// minSubArrayLen(4, [1, 4, 4]) -> 1 (подмассив [4])
// minSubArrayLen(11, [1, 1, 1, 1, 1, 1, 1, 1]) -> 0

const minSubArrayLen = (target, nums) => {
  // Решение тут
};

// Пример вызова:
console.log(minSubArrayLen(7, [2, 3, 1, 2, 4, 3]));          // 2
console.log(minSubArrayLen(4, [1, 4, 4]));                   // 1
console.log(minSubArrayLen(11, [1, 1, 1, 1, 1, 1, 1, 1])); // 0
