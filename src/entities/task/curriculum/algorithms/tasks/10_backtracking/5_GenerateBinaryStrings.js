// Напишите функцию generateBinaryStrings(n), которая возвращает все бинарные
// строки длины n. В каждой позиции можно выбрать только "0" или "1".
//
// Для n = 0 верните массив с одной пустой строкой: [""].

const generateBinaryStrings = (n) => {
  // Решение тут
};

// Пример вызова:
console.log(generateBinaryStrings(2)); // ["00", "01", "10", "11"]
console.log(generateBinaryStrings(1)); // ["0", "1"]
console.log(generateBinaryStrings(0)); // [""]
