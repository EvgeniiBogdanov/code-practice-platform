// Напишите функцию sortArrayByParity(nums), которая принимает массив целых чисел nums
// и переставляет элементы на месте (in-place) так, чтобы все чётные числа оказались перед нечётными.
//
// Функция должна возвращать измененный массив nums.
//
// Требования:
// 1. Изменять массив на месте (in-place) без создания нового массива (O(1) дополнительной памяти).
// 2. Временная сложность должна быть линейной O(n).
// 3. Порядок элементов внутри групп чётных и нечётных чисел значения не имеет.

const sortArrayByParity = (nums) => {
  // Решение тут
};

// Пример вызова:
console.log(sortArrayByParity([3, 1, 2, 4])); // [4, 2, 1, 3]
console.log(sortArrayByParity([0])); // [0]
console.log(sortArrayByParity([1, 2])); // [2, 1]
console.log(sortArrayByParity([2, 4, 6])); // [2, 4, 6]
console.log(sortArrayByParity([1, 3, 5])); // [1, 3, 5]
