// Напишите функцию orangesRotting(grid), которая принимает двумерную сетку grid размера m x n,
// где 0 — пустая клетка, 1 — свежий апельсин, 2 — гнилой апельсин.
//
// Каждую минуту любой свежий апельсин, соседствующий по 4 направлениям с гнилым, сгнивает.
// Функция должна вернуть минимальное количество минут, необходимое для того, чтобы не осталось свежих апельсинов.
// Если это невозможно, верните -1.

const orangesRotting = (grid) => {
  // Решение тут
};

// Пример вызова:
const grid1 = [
  [2, 1, 1],
  [1, 1, 0],
  [0, 1, 1]
];
console.log(orangesRotting(grid1)); // 4

const grid2 = [
  [2, 1, 1],
  [0, 1, 1],
  [1, 0, 1]
];
console.log(orangesRotting(grid2)); // -1

const grid3 = [
  [0, 2]
];
console.log(orangesRotting(grid3)); // 0
