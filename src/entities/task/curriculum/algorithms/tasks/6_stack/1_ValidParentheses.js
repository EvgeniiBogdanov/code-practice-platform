// Напишите функцию isValid(s), которая принимает строку s,
// состоящую только из символов скобок: '(', ')', '{', '}', '[' и ']'.
//
// Функция должна определить, является ли входная строка правильной (валидной).
//
// Строка считается правильной, если:
// 1. Открытые скобки закрываются скобками того же типа.
// 2. Открытые скобки закрываются в правильном порядке.
// 3. Каждая закрывающая скобка имеет соответствующую открывающую скобку того же типа.
//
// Примеры:
// isValid("()") -> true
// isValid("()[]{}") -> true
// isValid("(]") -> false
// isValid("([])") -> true

const isValid = (s) => {
  // Решение тут
};

// Пример вызова:
console.log(isValid("()"));     // true
console.log(isValid("()[]{}")); // true
console.log(isValid("(]"));     // false
console.log(isValid("([])"));   // true
