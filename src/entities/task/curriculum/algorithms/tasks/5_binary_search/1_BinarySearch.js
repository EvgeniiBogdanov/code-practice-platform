// Напишите функцию search(nums, target), которая принимает отсортированный
// по возрастанию массив уникальных целых чисел (nums) и искомое число (target).
//
// Функция должна найти индекс числа target в массиве nums и вернуть его.
// Если число target отсутствует в массиве, функция должна вернуть -1.
//
// Главное условие:
// Алгоритм должен работать за время O(log n) — то есть быстрее обычного
// перебора элементов с помощью цикла.
//
// Примеры:
// search([-1, 0, 3, 5, 9, 12], 9) -> 4 (число 9 находится по индексу 4)
// search([-1, 0, 3, 5, 9, 12], 2) -> -1 (числа 2 нет в массиве)

const search = (nums, target) => {
  // Решение тут
};

// Пример вызова:
console.log(search([-1, 0, 3, 5, 9, 12], 9)); // 4
console.log(search([-1, 0, 3, 5, 9, 12], 2)); // -1
console.log(search([5], 5));                   // 0
