// Напишите функцию generateParenthesis(n), которая принимает число n (количество пар скобок)
// и генерирует все возможные комбинации правильных (валидных) скобочных последовательностей.
//
// Скобочная последовательность считается правильной, если каждая открывающая скобка
// имеет парную закрывающую скобку и закрывающая скобка не идет раньше открывающей.

const generateParenthesis = (n) => {
  // Решение тут
};

// Пример вызова:
console.log(generateParenthesis(3)); // ["((()))", "(()())", "(())()", "()(())", "()()()"]
console.log(generateParenthesis(1)); // ["()"]
