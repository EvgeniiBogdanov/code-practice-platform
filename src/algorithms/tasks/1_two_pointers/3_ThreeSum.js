// Напишите функцию threeSum(nums), которая принимает массив целых чисел nums
// и возвращает все уникальные тройки [nums[i], nums[j], nums[k]],
// такие что i != j, i != k, j != k и nums[i] + nums[j] + nums[k] === 0.
//
// Требования:
// - Итоговый набор троек не должен содержать дубликатов
//   (например, [-1, 0, 1] и [0, -1, 1] считаются одинаковой тройкой
//   и должны попасть в ответ только один раз).
// - Порядок троек в ответе и порядок чисел внутри каждой тройки
//   значения не имеет.
// - Если подходящих троек нет — нужно вернуть пустой массив.

const threeSum = (nums) => {
  // Ваш код здесь
};

console.log(threeSum([-1, 0, 1, 2, -1, -4])); // [[-1, -1, 2], [-1, 0, 1]]
console.log(threeSum([0, 1, 1])); // []
console.log(threeSum([0, 0, 0])); // [[0, 0, 0]]
