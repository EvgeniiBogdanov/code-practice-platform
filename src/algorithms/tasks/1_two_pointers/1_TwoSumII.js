// Напишите функцию twoSum(numbers, target), которая принимает отсортированный по неубыванию
// массив целых чисел numbers и целевое число target. Функция должна найти два числа,
// сумма которых равна target, и вернуть их 1-based индексы в виде массива [index1, index2] (где 1 <= index1 < index2 <= numbers.length).
//
// Требования:
// 1. Использовать O(1) дополнительной памяти.
// 2. Входной массив гарантированно имеет ровно одно решение.
// 3. Один и тот же элемент нельзя использовать дважды.

const twoSum = (numbers, target) => {
  // Решение тут
};

// Пример вызова:
console.log(twoSum([2, 7, 11, 15], 9)); // [1, 2]
console.log(twoSum([2, 3, 4], 6));       // [1, 3]
console.log(twoSum([-1, 0], -1));        // [1, 2]
