// Напишите функцию subarraySum(nums, k), которая принимает массив целых чисел (nums)
// и целое число (k).
// Функция должна найти и вернуть общее количество непрерывных подмассивов,
// сумма элементов которых равна k.
//
// Подмассив — это непрерывная последовательность элементов внутри массива.
// Обратите внимание: числа в массиве могут быть как положительными, так и отрицательными или нулем.
//
// Примеры:
// subarraySum([1, 1, 1], 2) -> 2 (подмассивы [1, 1] с индексов 0..1 и 1..2)
// subarraySum([1, 2, 3], 3) -> 2 (подмассивы [3] и [1, 2])
// subarraySum([1, -1, 0], 0) -> 3 (подмассивы [1, -1], [0] и [1, -1, 0])

const subarraySum = (nums, k) => {
  // Решение тут
};

// Пример вызова:
console.log(subarraySum([1, 1, 1], 2));  // 2
console.log(subarraySum([1, 2, 3], 3));  // 2
console.log(subarraySum([1, -1, 0], 0)); // 3
