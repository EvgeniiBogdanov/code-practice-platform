// Напишите функцию pivotIndex(nums), которая принимает массив целых чисел (nums).
// Функция должна найти и вернуть "опорный" индекс (pivot index).
//
// Опорный индекс — это индекс элемента, для которого сумма всех чисел
// строго слева от него равна сумме всех чисел строго справа от него.
//
// Условия:
// - Если индекс находится на самом левом краю (индекс 0), сумма элементов слева равна 0.
// - Аналогично, для правого края сумма элементов справа равна 0.
// - Если таких индексов несколько, необходимо вернуть самый левый (наименьший) из них.
// - Если опорного индекса не существует, функция должна вернуть -1.
//
// Примеры:
// pivotIndex([1, 7, 3, 6, 5, 6]) -> 3
// (слева от индекса 3: 1 + 7 + 3 = 11, справа: 5 + 6 = 11)
// pivotIndex([1, 2, 3]) -> -1
// pivotIndex([2, 1, -1]) -> 0

const pivotIndex = (nums) => {
  // Решение тут
};

// Пример вызова:
console.log(pivotIndex([1, 7, 3, 6, 5, 6])); // 3
console.log(pivotIndex([1, 2, 3]));          // -1
console.log(pivotIndex([2, 1, -1]));         // 0
