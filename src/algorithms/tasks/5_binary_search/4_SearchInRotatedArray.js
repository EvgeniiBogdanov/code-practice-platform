// Напишите функцию search(nums, target), которая принимает
// отсортированный по возрастанию массив уникальных чисел (nums),
// который был сдвинут (повернут) в некоторой точке, и искомое число (target).
//
// Функция должна найти индекс числа target в массиве nums и вернуть его.
// Если число target отсутствует в массиве, функция должна вернуть -1.
//
// Пример повернутого массива:
// Исходный массив: [0, 1, 2, 4, 5, 6, 7]
// Повернутый массив: [4, 5, 6, 7, 0, 1, 2] (сдвиг произошел по индексу 4)
//
// Главное условие:
// Алгоритм должен работать за время O(log n) — с использованием модифицированного бинарного поиска.
//
// Примеры:
// search([4, 5, 6, 7, 0, 1, 2], 0) -> 4
// search([4, 5, 6, 7, 0, 1, 2], 3) -> -1
// search([1], 0)                   -> -1

const search = (nums, target) => {
  // Решение тут
};

// Пример вызова:
console.log(search([4, 5, 6, 7, 0, 1, 2], 0)); // 4
console.log(search([4, 5, 6, 7, 0, 1, 2], 3)); // -1
console.log(search([1], 0));                   // -1
