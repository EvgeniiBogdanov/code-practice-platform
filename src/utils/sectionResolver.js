/**
 * Re-exporting from central registry for compatibility
 */
export {
  resolveTaskSection,
  getTaskById,
  ALL_TASKS,
  ALL_REACT_TASKS,
  ALL_JS_TASKS,
  ALL_ALGO_TASKS,
  TASKS_BY_ID,
} from "../data/tasksRegistry";
